import React from 'react';

const Quote = () => {
  const quoteStyle = {
    fontSize: '3rem',
  };

  return (
    <div style={quoteStyle}>
      <p>Quote here!</p>
    </div>
  );
};

export default Quote;
